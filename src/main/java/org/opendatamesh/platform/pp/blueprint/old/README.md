# Backward compatibility package (`old`)

Temporary adapters that keep Blueprint working with older product-plane services.

Current contents:

- [`v1/`](v1/README.md) — Policy Service V1 compatibility for protected-resources evaluation

**Core MUST NOT depend on this package.** Delete the tree when the corresponding V2 path exists.
