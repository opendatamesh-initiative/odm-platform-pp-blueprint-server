# Architecture Notes

This is a richer source repository fixture used by integration tests.

- `templates/` contains templated assets.
- `infrastructure/core/` contains static files.
- `README.md` is expected to be excluded by instantiation flow.
