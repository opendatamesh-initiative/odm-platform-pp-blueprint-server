spring:
  config:
    activate:
      on-profile: test
  jpa:
    properties:
      hibernate:
        show_sql: false
        default_schema: odm_blueprint_test
        dialect: org.hibernate.dialect.PostgreSQLDialect

odm:
  product-plane:
    policy-service:
      active: true